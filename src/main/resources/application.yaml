server:
  port: 8080
management:
  endpoints:
    web:
      exposure:
        include:
          - health
spring:
  data:
    mongodb:
      uri: mongodb://${MONGO_DB_USER}:${MONGO_DB_PASSWORD}@${MONGO_DB_ADDRESS}:27017/hello-world